management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    env:
      post:
        enabled: true
      show-values: always
    configprops:
      show-values: always

logging:
  level:
    root: debug

author:
  name: Gianmarco Barca
