management:
  endpoint:
    env:
      show-values: always
    configprops:
      show-values: always
  endpoints:
    web:
      exposure:
        include: info, health, loggers, refresh, env, configprops, beans

logging:
  level:
    org.springframework.cloud: info

author:
  name: Gianmarco Barca
